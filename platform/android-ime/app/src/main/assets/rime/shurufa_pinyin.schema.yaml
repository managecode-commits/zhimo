schema:
  schema_id: shurufa_pinyin
  name: Shurufa 简体拼音
  version: "1.0"
  author:
    - Shurufa Contributors
  description: Shurufa 全拼方案，使用离线简体拼音词典并启用本地用户词典

switches:
  - name: ascii_mode
    reset: 0
    states: [中文, ABC]

engine:
  processors:
    - ascii_composer
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - script_translator
  filters:
    - uniquifier

speller:
  alphabet: abcdefghijklmnopqrstuvwxyz
  delimiter: " '"

translator:
  dictionary: pinyin_simp
  enable_user_dict: true
  enable_sentence: true
  enable_completion: true

punctuator:
  half_shape:
    ',': '，'
    '.': '。'
    '?': '？'
    '!': '！'

key_binder:
  bindings:
    - { when: has_menu, accept: minus, send: Page_Up }
    - { when: has_menu, accept: equal, send: Page_Down }

recognizer:
  patterns:
    uppercase: "[A-Z][-_+.'0-9A-Za-z]*$"
