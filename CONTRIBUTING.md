<!-- Copyright © 2026 立方田 &lt;managecode@gmail.com&gt; -->
# Contributing to Zhimo

欢迎提交 Issue 和 Pull Request。工程技术标识统一使用 `zhimo` / `Zhimo` / `ZHIMO`。后续修改包名、ABI、用户数据目录或平台注册 ID 时，必须明确兼容性和迁移方案。

1. 描述复现步骤、平台与版本、预期结果及实际结果；手写问题请注明目标文字。
2. 确认你有权公开所提交的代码、笔迹和截图。请裁去聊天内容、账号等个人信息；不要提交密码、密钥、真实学习词库或含敏感文字的日志。
3. 遵循现有代码风格，运行 `./tools/verify.sh`；Android 改动补充构建和相关设备测试。完整命令见 `docs/构建与验收.md`。
4. 说明测试覆盖和未验收边界；不能用合成或训练集样例代表真实用户准确率。
5. 新模型或词库必须提供出处、版本、校验值、授权文本及必要的对应源数据；不得通过删除授权文件缩小包体。

自有代码贡献遵循项目 Apache-2.0 许可；第三方衍生代码和数据继续遵守其适用许可。安装包应作为单独的发布产物，不提交到 Git 源码历史。

## 版权声明

项目作者为立方田，联系邮箱为 managecode@gmail.com，统一声明见根目录 `NOTICE`。
自有代码、脚本、构建配置和文档保留文件级版权注释；新增文件后运行
`python3 tools/copyright-headers.py` 检查，或使用 `--apply` 补充缺失声明。
脚本保留 shebang、XML 声明、编码标记、换行格式及已有内容。
第三方代码、模型、词库、生成文件、Gradle Wrapper 和 BSD 衍生的 Zinnia 实现不批量添加项目署名；
无注释格式的自有 JSON 等配置由 `NOTICE` 统一说明，原有许可证不作替换。
其他贡献者可保留或增加自己的版权声明，不要求将第三方或个人贡献的权利转让给项目作者。
